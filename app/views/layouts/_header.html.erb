  <header class="navbar navbar-fixed-top navbar-inverse">
    <div class="container">
      <%= link_to "1G.jpg", root_path, id: "logo" %>
      <nav>
        <ul class="nav navbar-nav navbar-right">
          <li> <%= link_to "主页", root_path %>
          <li> <%= link_to "关于", about_path %>
          <li><%= link_to "上传作品", new_work_path %> </li>
        <% if logged_in? %>
          <% if current_user.admin?%>
          <li><%= link_to "Users", users_path %>
          <% end %>
          <li class="dropdown">
            <a href="#" class="dropdown-toggle" data-toggle="dropdown">
              账户<b class="caret"> </b>
            </a>
            <ul class="dropdown-menu">
              <li><%= link_to "我的主页",current_user %></li>
              <li><%= link_to "设置", edit_user_path(current_user) %></li>
              <li class="divider"></li>
              <li>
                <%=link_to "注销",logout_path, method: "delete" %>
              </li>
            </ul>
            </li>

            <% else %>
              <li><%= link_to "登录", login_path %></li>
              <li> <%= link_to "注册", signup_path%>
          <% end %>
        </ul>
      </nav>
      <div class="menu-o ">
          <button type="button" class="menu-btn-o mobile-only btn btn-default ">
            <span class="glyphicon glyphicon-list">
          </button>
      </div>

      <div class="menu-c ">
          <button type="button" class="menu-btn-c mobile-only btn btn-default">
            <span class="glyphicon glyphicon-list">
          </button>
      </div>

  </div><!-- end continer -->
  </header>
      <div class="mobile-menu mobile-only" >
          <ul>
              <li><%=link_to '首页', root_path,data:{no_turbolink: true} %></li>
              <li><%=link_to '上传', new_work_path, data:{no_turbolink: true} %></li>

              <% if logged_in? %>

                  <li><%=link_to '我', current_user,data:{no_turbolink: true} %></li>
                  <li><%=link_to '设置', edit_user_path(current_user), data:{no_turbolink: true } %></li>
                  <li><%=link_to '注销', logout_path, method: "delete" , data:{no_turbolink: true }%></li>
              <% else %>
                  <li><%=link_to '登录', login_path, data: { no_turbolink: true }%></li>
                  <li><%=link_to '注册', signup_path, data: { no_turbolink: true } %></li>
              <% end %>

          </ul>
      </div>

